<form method="post" action="{{ path('company_executive_delete', {'id': company_executive.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_executive.id) }}">
    <button class="btn">Delete</button>
</form>
