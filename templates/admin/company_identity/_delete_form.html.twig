<form method="post" action="{{ path('company_identity_delete', {'id': company_identity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_identity.id) }}">
    <button class="btn">Delete</button>
</form>
